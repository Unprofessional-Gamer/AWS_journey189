aws:
  region: eu-north-1
  bucket_name: catalog-learning-project
  role_name: GlueFullAccessRole         # used by infra/create_infra.py
  role_arn: arn:aws:iam::980805786648:role/GlueFullAccessRole  # used by schema_to_glue_dynamic.py

glue:
  landing_db: landing_db
  fdp_db: fdp_db
  glue_version: "3.0"
  temp_dir: s3://catalog-learning-project/temp/
  dynamic_job_name: fdp_writer_job

s3_paths:
  source_prefix: source_zone/
  target_prefix: fdp_zone/
  script_prefix: scripts/   # ✅ add this line

